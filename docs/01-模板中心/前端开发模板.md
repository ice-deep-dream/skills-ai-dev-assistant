---
title: 前端开发模板
description: 前端模块开发模板（初始化时基于项目生成通用工具信息）
category: 模板
tags:
  - 前端
  - 开发
outline: deep
---

# 【模块名称】前端开发文档

> **核心原则**：模板服务于项目，项目优先于模板
> 
> **重要**：此模板在 `auto-init` 时会自动填充项目通用工具信息

---

## 一、项目分析

### 1.1 技术栈

| 项目特征 | 分析结果 |
|---------|---------|
| **框架** | 【自动检测：Vue/React】 |
| **构建工具** | 【自动检测：Vite/Webpack】 |
| **状态管理** | 【自动检测：Pinia/Vuex/Redux】 |
| **UI 组件库** | 【自动检测】 |
| **代码风格** | 【自动检测：TypeScript/JavaScript】 |

### 1.2 目录结构

```
【初始化时自动检测项目目录结构】
```

---

## 二、项目通用工具（auto-init 时生成）

> ⚠️ 以下内容在初始化时自动扫描项目并填充

### 2.1 API 请求封装

**封装位置**：`【初始化时自动检测】`

```typescript
// 【初始化时根据项目实际代码生成】
// 例如：src/utils/request.ts

import axios from 'axios'

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10000,
})

// 请求拦截器
request.interceptors.request.use((config) => {
  // 【项目实际的拦截逻辑】
  return config
})

// 响应拦截器
request.interceptors.response.use((response) => {
  // 【项目实际的响应处理】
  return response.data
})

export default request
```

**API 模块示例**：
```typescript
// 【初始化时根据项目实际API组织方式生成】
import request from '@/utils/request'

export const moduleApi = {
  getList(params) { return request.get('/api/module', { params }) },
  getDetail(id) { return request.get(`/api/module/${id}`) },
  create(data) { return request.post('/api/module', data) },
  update(id, data) { return request.put(`/api/module/${id}`, data) },
  delete(id) { return request.delete(`/api/module/${id}`) },
}
```

### 2.2 状态管理封装

**封装位置**：`【初始化时自动检测】`

```typescript
// 【Pinia 风格 - 初始化时根据项目实际方案生成】
import { defineStore } from 'pinia'

export const useModuleStore = defineStore('module', () => {
  const data = ref([])
  const loading = ref(false)
  
  async function fetchList() {
    loading.value = true
    try {
      data.value = await moduleApi.getList()
    } finally {
      loading.value = false
    }
  }
  
  return { data, loading, fetchList }
})
```

### 2.3 通用组件

**组件目录**：`【初始化时自动检测】`

| 组件名 | 用途 | 初始化时扫描项目填充 |
|--------|------|-------------------|
| 【自动扫描填充】 | 【用途说明】 | 【使用示例】 |

### 2.4 工具函数

**工具目录**：`【初始化时自动检测】`

| 函数名 | 用途 | 初始化时扫描项目填充 |
|--------|------|-------------------|
| 【自动扫描填充】 | 【用途说明】 | 【使用示例】 |

### 2.5 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件文件 | 【初始化检测】 | `UserList.vue` |
| API文件 | 【初始化检测】 | `userApi.ts` |
| Store文件 | 【初始化检测】 | `userStore.ts` |

---

## 三、模块概述

### 3.1 模块目标

**功能清单**：
- [ ] 【功能1】
- [ ] 【功能2】
- [ ] 【功能3】

### 3.2 依赖关系

**上游依赖**：- [ ] 【依赖模块】

**下游依赖**：- [ ] 【被依赖模块】

---

## 四、开发内容

### 4.1 文件结构

```
【本模块文件结构】
```

### 4.2 组件开发

```vue
<!-- 【根据项目实际组件风格编写】 -->
<template>
  <!-- 组件模板 -->
</template>

<script setup lang="ts">
// 【使用项目实际的组件风格】
</script>

<style scoped lang="scss">
/* 【使用项目实际的样式方案】 */
</style>
```

### 4.3 状态管理

```typescript
// 【使用项目实际的状态管理方案】
// 参考第二节中的状态管理封装
```

### 4.4 API调用

```typescript
// 【使用第二节中的API封装方式】
// 参考项目实际的API模块组织方式
```

---

## 五、开发进度

| 状态 | 进度 | 负责人 | 开始时间 | 完成时间 |
|------|------|--------|---------|---------|
| 开发中 | 0% | 【负责人】 | 【日期】 | - |

---

## 六、更新记录

| 日期 | 内容 | 更新人 |
|------|------|--------|
| 【日期】 | 创建文档 | 【姓名】 |

---

## 七、风险备注

【记录开发过程中的风险和问题】

---

**使用说明**：

1. `auto-init` 时自动填充第二节（项目通用工具）
2. 开发新模块时复制此模板，填写第三节和第四节
3. 保持与项目现有封装一致，不要重复造轮子