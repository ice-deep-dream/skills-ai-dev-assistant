---
title: 后端开发模板
description: 后端模块开发模板（初始化时基于项目生成通用工具信息）
category: 模板
tags:
  - 后端
  - 开发
outline: deep
---

# 【模块名称】后端开发文档

> **核心原则**：模板服务于项目，项目优先于模板
> 
> **重要**：此模板在 `auto-init` 时会自动填充项目通用工具信息

---

## 一、项目分析

### 1.1 技术栈

| 项目特征 | 分析结果 |
|---------|---------|
| **框架** | 【自动检测：NestJS/Express/Koa】 |
| **数据库** | 【自动检测：MySQL/PostgreSQL/MongoDB】 |
| **ORM** | 【自动检测：TypeORM/Prisma/Sequelize】 |
| **API规范** | 【自动检测：RESTful/GraphQL】 |
| **代码风格** | 【自动检测：TypeScript/JavaScript】 |

### 1.2 目录结构

```
【初始化时自动检测项目目录结构】
```

---

## 二、项目通用工具（auto-init 时生成）

> ⚠️ 以下内容在初始化时自动扫描项目并填充

### 2.1 API 返回封装

**封装位置**：`【初始化时自动检测】`

```typescript
// 【NestJS风格 - 初始化时根据项目实际代码生成】
export class ApiResponse<T> {
  code: number
  message: string
  data: T
  timestamp: number

  static success<T>(data: T, message = '操作成功') {
    return { code: 200, message, data, timestamp: Date.now() }
  }

  static error(code: number, message: string) {
    return { code, message, data: null, timestamp: Date.now() }
  }
}
```

```javascript
// 【Express风格 - 初始化时根据项目实际代码生成】
class ApiResponse {
  static success(res, data, message = '操作成功') {
    return res.json({ code: 200, message, data, timestamp: Date.now() })
  }
  static error(res, code, message) {
    return res.status(code).json({ code, message, data: null, timestamp: Date.now() })
  }
}
```

### 2.2 分页工具

**封装位置**：`【初始化时自动检测】`

```typescript
// 【初始化时根据项目实际ORM生成分页工具】
export interface PaginationResult<T> {
  list: T[]
  total: number
  page: number
  pageSize: number
  totalPages: number
}

export async function paginate<T>(
  repository: Repository<T>,
  { page = 1, pageSize = 10, keyword }
): Promise<PaginationResult<T>> {
  const query = repository.createQueryBuilder('entity')
  if (keyword) {
    query.where('entity.name LIKE :keyword', { keyword: `%${keyword}%` })
  }
  const [list, total] = await query
    .skip((page - 1) * pageSize)
    .take(pageSize)
    .getManyAndCount()
  return { list, total, page, pageSize, totalPages: Math.ceil(total / pageSize) }
}
```

### 2.3 认证中间件

**封装位置**：`【初始化时自动检测】`

```typescript
// 【NestJS Guard - 初始化时根据项目实际代码生成】
@Injectable()
export class AuthGuard implements CanActivate {
  canActivate(context: ExecutionContext): boolean {
    const request = context.switchToHttp().getRequest()
    // 【项目实际的认证逻辑】
    return !!request.user
  }
}
```

```javascript
// 【Express中间件 - 初始化时根据项目实际代码生成】
function authMiddleware(req, res, next) {
  const token = req.headers.authorization?.split(' ')[1]
  if (!token) return res.status(401).json({ code: 401, message: '未登录' })
  try {
    req.user = jwt.verify(token, process.env.JWT_SECRET)
    next()
  } catch (e) {
    return res.status(401).json({ code: 401, message: 'Token无效' })
  }
}
```

### 2.4 错误处理

**封装位置**：`【初始化时自动检测】`

```typescript
// 【初始化时根据项目实际方案生成】
// NestJS: Exception Filter
// Express: 错误处理中间件
```

### 2.5 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| Controller | 【初始化检测】 | `UserController` |
| Service | 【初始化检测】 | `UserService` |
| DTO | 【初始化检测】 | `CreateUserDto` |
| 实体 | 【初始化检测】 | `User` |
| 表名 | 【初始化检测】 | `users` |

---

## 三、模块概述

### 3.1 模块目标

**功能清单**：
- [ ] 【功能1】
- [ ] 【功能2】
- [ ] 【功能3】

### 3.2 依赖关系

**上游依赖**：- [ ] 【依赖模块】

**下游依赖**：- [ ] 【被依赖模块】

### 3.3 数据库设计

**表名**: `【table_name】`

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | VARCHAR(36) | 是 | 主键 |
| name | VARCHAR(100) | 是 | 名称 |

---

## 四、开发内容

### 4.1 文件结构

```
【本模块文件结构】
```

### 4.2 实体定义

```typescript
// 【使用项目实际ORM风格】
// 参考第二节中的命名规范
```

### 4.3 接口开发

```typescript
// 【使用项目实际框架风格】
// 参考第二节中的API返回封装和分页工具
```

### 4.4 业务逻辑

```typescript
// 【Service层实现】
```

---

## 五、开发进度

| 状态 | 进度 | 负责人 | 开始时间 | 完成时间 |
|------|------|--------|---------|---------|
| 开发中 | 0% | 【负责人】 | 【日期】 | - |

---

## 六、更新记录

| 日期 | 内容 | 更新人 |
|------|------|--------|
| 【日期】 | 创建文档 | 【姓名】 |

---

## 七、风险备注

【记录开发过程中的风险和问题】

---

**使用说明**：

1. `auto-init` 时自动填充第二节（项目通用工具）
2. 开发新模块时复制此模板，填写第三节和第四节
3. 使用统一的API返回格式和分页工具
4. 保持与项目现有封装一致