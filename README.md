# AI 开发助手

> 辅助 AI 进行开发文档编写的 Skill

## 核心理念

1. **模板服务于项目** - 项目优先于模板
2. **自动填充通用工具** - 初始化时扫描项目代码，自动填充模板
3. **简化结构** - 合并目录，减少文件数量
4. **自动记录** - 模块开始/结束自动记录，过程询问用户

## 核心功能

### 智能初始化 `auto-init`

一键完成所有初始化工作：

```
1. 创建 docs 目录结构
   ├── 01-模板中心/
   ├── 02-开发计划/
   └── 03-项目跟踪/
       ↓
2. 分析项目技术栈
   - 检测框架（Vue/React/NestJS/Express等）
   - 检测目录结构
   - 检测命名规范
       ↓
3. 扫描项目通用工具
   - API 请求封装
   - 状态管理方案
   - 通用组件列表
   - 工具函数列表
       ↓
4. 填充模板第二节（项目通用工具）
   - 前端：API封装、Store示例、组件列表
   - 后端：API返回封装、分页工具、中间件
       ↓
5. 完成，可开始开发
```

**使用场景**: 模块开始时、模块结束时

---

## 目录结构

```
docs/
├── .vitepress/          # VitePress 配置
├── 01-模板中心/         # 开发模板 + 通用工具信息
│   ├── index.md
│   ├── 前端开发模板.md   # 含自动填充的通用工具
│   └── 后端开发模板.md   # 含自动填充的通用工具
├── 02-开发计划/         # 前后端开发文档
│   └── index.md
├── 03-项目跟踪/         # Bug/测试/总结/进度
│   └── index.md
└── index.md
```

---

## 使用流程

### 初始化流程（模块开始/结束）

```
调用 auto-init → 创建目录 → 扫描项目 → 填充模板 → 开始开发
```

### 日常开发流程

```
复制模板 → 参考通用工具 → 开发代码 → 询问是否记录 → 完成
```

---

## 命令列表

| 命令 | 功能 | 执行时机 |
|------|------|---------|
| `auto-init` | 智能初始化 | 模块开始/结束 |
| `update-vitepress-config` | 更新侧边栏 | 新增文档后 |
| `update-index-files` | 更新进度表格 | 状态变更后 |

---

## 文档规范

### Frontmatter 规范

```yaml
---
title: 文档标题
description: 文档描述
category: 分类
tags: [标签]
status: developing
progress: 0-100
owner: 负责人
created: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

### 命名规范

| 类型 | 格式 | 示例 |
|------|------|------|
| 开发文档 | `001-模块开发.md` | `001-用户管理开发.md` |
| Bug 文档 | `BUG-001-标题.md` | `BUG-001-登录失败.md` |
| 测试文档 | `TEST-001-名称.md` | `TEST-001-用户测试.md` |
| 总结文档 | `总结-YYYYMMDD-主题.md` | `总结-20240115-复盘.md` |

---

## VitePress 站点

```bash
cd docs
npm install
npm run docs:dev    # 预览
npm run docs:build  # 构建
```

---

## 核心原则

1. **模板服务于项目** - 项目优先于模板
2. **保持一致性** - 使用项目现有封装
3. **自动填充** - 减少手动工作
4. **简化结构** - 便于管理
5. **自动记录** - 模块开始/结束自动记录

---

**让文档成为开发的助力！**